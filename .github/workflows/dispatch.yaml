name: dispatch-test

on:
  workflow_dispatch: 

jobs:
  python-test:
    runs-on: ubuntu-latest      
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: run python
        run: | 
          python3 ./test/app.py
 